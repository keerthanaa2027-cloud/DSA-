 unordered_map<int, int> mp;

        mp[0] = 1; // Empty prefix

        int prefixSum = 0;
        int count = 0;

        for (int num : nums) {
            prefixSum += num;

            int rem = prefixSum % k;

            // Handle negative remainder
            if (rem < 0)
                rem += k;

            if (mp.find(rem) != mp.end()) {
                count += mp[rem];
            }

            mp[rem]++;
        }

        return count;
